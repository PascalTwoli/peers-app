# URL of the marketing/landing site. The app's "/" route redirects here.
VITE_MARKETING_URL=https://peers-hub.vercel.app

# Backend API and WebSocket URL (if not using the auto-detect in serverConfig.js)
# VITE_API_URL=https://your-railway-backend.up.railway.app
